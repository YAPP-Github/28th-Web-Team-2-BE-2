interface:
  display_name: "To Spec"
  short_description: "Turn established context into a specification"
  default_prompt: "Use $to-spec to synthesize our decisions into a buildable specification."
policy:
  allow_implicit_invocation: false
