interface:
  display_name: "To Tickets"
  short_description: "Split work into vertical implementation tickets"
  default_prompt: "Use $to-tickets to split this work into approved tracer-bullet tickets with dependencies."
policy:
  allow_implicit_invocation: false
