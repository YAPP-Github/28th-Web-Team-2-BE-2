spring:
  application:
    name: demo
  config:
    import:
      - classpath:application-kamis-client.yml
  jpa:
    hibernate:
      ddl-auto: validate
item:
  price:
    collection:
      selenium:
        headless: false
        page-load-timeout: 30s
        wait-timeout: 10s
